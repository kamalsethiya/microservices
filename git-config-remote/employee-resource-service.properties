#server.servlet.context-path=/employee

# ================================================
# = TOMCAT AND APP LEVEL CONFIGURATION 
# =================================================
server.port=8001


# This controls which Actuator web endpoints are exposed.  
# By default, only /actuator/health and /actuator/info are exposed, 
# But we need to allow external parties to POST to /refresh:    
management.endpoints.web.exposure.include=health,info,refresh

employee.label=employee-name